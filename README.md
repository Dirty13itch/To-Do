# To-Do (Vite + React)

## Run locally
1) Install deps:
   npm.cmd install
2) Start dev server:
   npm.cmd run dev

Then open the URL printed in the terminal (usually http://localhost:5173).

## Artifact source
Original Claude artifact is preserved in:
- claude-artifact/todo-app.jsx
